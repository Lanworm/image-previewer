events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /images/ {
            alias /usr/share/nginx/html/images/;
            autoindex on;
        }
    }
}
